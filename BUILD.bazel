load("@rules_rust//rust:defs.bzl", "rust_binary")

rust_binary(
    name = "hello_workspace",
    srcs = ["src/main.rs"],
    edition = "2021",
    deps = [
        "@crates//:reqwest",
        "@crates//:scraper",
    ],
)
